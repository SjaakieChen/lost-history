# Landscape backgrounds

Place **21:9 panoramic** images here for the landscape viewer (e.g. `default.png`).

Development uses `default.svg` as a placeholder. Replace with a PNG/JPEG for production:

```
public/landscapes/default.png
```

Then set `backgroundUrl: '/landscapes/default.png'` in your scene snapshot.
